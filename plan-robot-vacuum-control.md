# 计划：玩家轮盘控制扫地机器人

> 创建时间：2026-07-26 | 范围：系统级 | 实施方式：分 3 个阶段，每阶段完成后暂停，交由用户在引擎侧验收

## 目标

场景中已有名为 `扫地机器人` 的 unit，以及 EUI 节点 `robot_start_btn`。玩家举起并放下机器人后，机器人以 Z 轴 `90.00°` 的姿态贴地；按钮对该玩家显示。点击按钮后，玩家角色被禁止移动，轮盘输入转而驱动扫地机器人的移动和朝向，机器人位移使用 `set_position_smooth`。

## 已核实的技术依据

| 能力 | 采用接口 / 事件 |
|------|-----------------|
| 查找机器人 | `LuaAPI.query_unit("扫地机器人")` |
| 监听举起 / 放下 | `SPEC_OBSTACLE_LIFTED_BEGIN` / `SPEC_OBSTACLE_LIFTED_END` |
| 找到操作玩家 | 事件 `data.lift_unit` → `RoleUtil.get_role_by_unit` |
| 固定贴地姿态 | `unit.set_orientation(math.Quaternion(0, yaw, math.deg_to_rad(90.0)))` |
| 读取轮盘方向 | 玩家控制单位的 `get_joystick_direction()` |
| 平滑移动 | `robot.set_position_smooth(position)` |
| 禁止玩家移动 | 玩家控制单位 `add_state/remove_state(Enums.BuffState.BUFF_FORBID_MOVE)` |
| 按钮表现 | `Data.UINodes.robot_start_btn`，由 View 层按玩家设置可见性和文字 |
| 连续运动 | `Core/Drivers` 内通过 `Timer.every_frame` 按帧推进 |

## 架构落点

- `BabyStorm/RobotVacuumController.lua`
  - 负责机器人控制会话、阶段切换、举放事件与清理编排。
- `BabyStorm/Core/Drivers/RobotDriveDriver.lua`
  - 唯一连续移动入口；每帧读取轮盘方向，用 `set_position_smooth` 移动并更新朝向。
- `BabyStorm/View/RobotVacuumView.lua`
  - 唯一引用 `Data.UINodes.robot_start_btn` 的新模块；负责按玩家显示、隐藏和切换按钮文案。
- `BabyStorm/Config/BabyStormConfig.lua`
  - 保存机器人名称、移动速度、转向速度、输入死区、Z 轴贴地角度等可调参数。
- `App/ControllerRegistry.lua`
  - 注册新的独立控制器，不把扫地机器人逻辑塞进宝宝 AI 状态机。

## UI / 交互方案（待审阅）

- 不创建新 UI，完全复用已有 `robot_start_btn`，保留编辑器里现有样式和布局。
- 游戏开始时按钮隐藏。
- 玩家放下 `扫地机器人` 后，仅对该玩家显示按钮，文案为“启动机器人”。
- 玩家点击后进入控制态，按钮继续显示并切换为“退出控制”。
- 控制态再次点击按钮，主动退出并立即恢复玩家移动，文案恢复为“启动机器人”。
- 机器人被其他玩家举起、控制者失效或玩法销毁时，强制退出控制并解除 Buff。
- 风格建议：这是常驻功能 HUD，沿用现有按钮样式，不新增弹窗或遮罩，避免遮挡轮盘和场景视野。

## 推荐的首版参数

| 参数 | 建议值 | 说明 |
|------|--------|------|
| 移动速度 | `6.0` 单位/秒 | 先按中等速度实现，验收后再调 |
| 自转速度 | `3.0` 弧度/秒 | 启动后绕世界 Y 轴持续自转，退出时停止 |
| 输入死区 | `0.10` | 防止轮盘松手后轻微漂移 |
| 驱动频率 | 每 `1` 帧 | 项目为 30 FPS，位移按 `1 / 30` 秒换算 |
| 贴地角度 | Z 轴 `90.00°` | Y 轴用于控制行驶朝向 |
| 高度策略 | 锁定控制开始时的 Y 值 | 轮盘只改变 X/Z，防止机器人上下漂移 |

## 阶段 1：放下贴地与按钮出现

### 实现内容

- [x] 新建机器人 Controller 与 View 骨架，并注册到 `ControllerRegistry`
- [x] 启动时按名称查找 `扫地机器人`；找不到时只记录警告，不影响宝宝玩法
- [x] 启动时对所有当前玩家隐藏 `robot_start_btn`
- [x] 监听机器人的举起事件：记住举起者，并隐藏旧的操作入口
- [x] 监听机器人的放下事件：
  - 将机器人姿态设置为 Z 轴 `90.00°`
  - 将该玩家记为当前可启动者
  - 仅向该玩家显示“启动机器人”按钮

### 阶段 1 验收

- [x] WHEN 进入游戏但未放下机器人，THEN `robot_start_btn` 不显示
- [x] WHEN 玩家举起并放下机器人，THEN 机器人以 Z=`90.00°` 贴地
- [x] WHEN 放下完成，THEN 放下机器人的玩家看到“启动机器人”按钮
- [x] WHEN 另一名玩家再次举起机器人，THEN 旧玩家的按钮入口被隐藏

> 阶段门：完成后停止编码，等待用户在引擎侧确认姿态和按钮显示。

## 阶段 2：轮盘控制移动与转向

### 实现内容

- [x] 点击“启动机器人”后建立单一控制会话
- [x] 对控制者的玩家单位幂等添加 `BUFF_FORBID_MOVE`
- [x] 启动 `RobotDriveDriver`
- [x] 每帧调用玩家单位 `get_joystick_direction()`
- [x] 死区外：
  - 将轮盘 X/Z 归一化为世界平面方向
  - 按 `speed / 30` 计算该帧目标位置
  - 保持控制开始时的 Y 值
  - 调用 `robot.set_position_smooth(target_position)`
  - 初版曾朝轮盘方向平滑更新 Y 轴朝向；阶段 2.5 为保证恒速自转，改为轮盘只控制位移
- [x] 死区内：不改变位置，不更新朝向
- [x] 按钮文案切换为“退出控制”
- [x] 再次点击按钮时停止驱动、移除本功能添加的 Buff、恢复“启动机器人”

### 阶段 2 验收

- [x] WHEN 点击“启动机器人”，THEN 玩家角色不能移动，但轮盘仍能被读取
- [x] WHEN 向任意方向推动轮盘，THEN 机器人平滑向对应方向移动
- [x] WHEN 改变轮盘方向，THEN 机器人按新方向移动且持续保持贴地
- [x] WHEN 松开轮盘，THEN 机器人停止，不发生缓慢漂移
- [x] WHEN 点击“退出控制”，THEN 机器人停止且玩家立即恢复移动

> 阶段门：完成后停止编码，等待用户在引擎侧确认速度、转向手感和退出闭环。

## 阶段 2.5：启动后的持续自转

### 实现内容

- [x] 配置 `spin_speed = 3.0` 弧度/秒
- [x] Driver 每帧独立累加自转相位
- [x] 最终 Y 轴显示朝向只使用自转相位，轮盘不再叠加视觉转向
- [x] 左右切换时自转角速度恒定，不出现短暂加速、减速或反转
- [x] 轮盘处于死区时只自转，不改变位置
- [x] 退出控制、机器人被举起或 Driver 停止时停止自转
- [x] 放下机器人时清零自转相位并恢复 Z=`90.00°`

### 阶段 2.5 验收

- [x] WHEN 点击“启动机器人”且不推动轮盘，THEN 机器人在原地绕世界 Y 轴持续自转
- [x] WHEN 自转过程中推动轮盘，THEN 机器人仍按轮盘方向移动
- [x] WHEN 机器人持续自转，THEN Z 轴仍保持 `90.00°` 贴地
- [x] WHEN 点击“退出控制”或机器人被举起，THEN 自转立即停止且玩家恢复移动

> 阶段门：完成后停止编码，等待用户确认自转轴向与速度，再进入阶段 3。

## 阶段 2.6：运动碰撞与污渍清理

### 实现内容

- [x] 放下及启动控制时显式开启机器人物理与 CCD
- [x] `RobotDriveDriver` 在 `set_position_smooth` 前用三条水平射线近似扫掠机器人宽度
- [x] 检测到普通障碍物时收回逻辑目标并停止继续穿入
- [x] 脏尿布本体不作为移动阻挡物，由机器人接触后直接清除
- [x] `DirtyDiaperProp` 提供 `is_cleanable`、`clean_unit`、`clean_near`
- [x] 脏尿布本体清除后停止继续生成溅射，但保留既有溅射等待继续清扫
- [x] 无物理碰撞的 Decoration 溅射物使用 XZ 清洁半径检测并逐块清除
- [x] 只有机器人处于启动控制状态时才执行清洁

### 阶段 2.6 验收

- [ ] WHEN 机器人驶向墙体或普通组件，THEN 在碰撞体前停止，不再直接穿透
- [ ] WHEN 机器人接触脏尿布本体，THEN 该尿布消失且不再生成新溅射
- [ ] WHEN 机器人扫过尿布产生的溅射贴花，THEN 接触范围内的贴花逐块消失
- [ ] WHEN 机器人未启动但被放在污渍旁，THEN 不会自动清除污渍
- [ ] WHEN 机器人清理污渍，THEN 玩家禁移、轮盘移动、恒速自转和退出功能保持正常

> 阶段门：完成后停止编码，等待用户确认碰撞阻挡与清洁范围，再进入阶段 3。

## 阶段 3：多人占用与异常清理

### 实现内容

- [ ] 同一时间只允许一名玩家控制机器人
- [ ] 非控制者点击时不抢占控制，可显示轻提示“机器人正在使用中”
- [x] 控制期间机器人被举起时，立即退出控制并解除控制者 Buff（安全项已提前至阶段 2）
- [ ] 控制者离线、角色单位失效、机器人失效或游戏销毁时，停止 Driver 并解除 Buff
- [x] `destroy` 清空按钮状态、控制会话和本功能持有的帧定时器（安全项已提前至阶段 2）
- [ ] 保证重复进入 / 退出不会重复叠加或多移除 `BUFF_FORBID_MOVE`

### 阶段 3 验收

- [ ] WHEN A 正在控制且 B 尝试启动，THEN B 不能抢占，A 控制不受影响
- [ ] WHEN 控制中机器人被举起，THEN 控制立即结束，玩家恢复移动
- [ ] WHEN 控制功能被销毁或异常退出，THEN 无永久禁移、无残留帧驱动
- [ ] WHEN 连续多次启动和退出，THEN 每次都能正确禁移与恢复

> 阶段门：完成后停止编码，等待用户最终回归验收。

## 明确不在首版范围

- 不自动切换或绑定玩家相机；继续使用现有游戏视角。
- 不创建额外摇杆，直接复用玩家原始轮盘输入。
- 不新增机器人动画、音效、特效或电量系统。
- 不由 Codex 启动引擎试玩；每阶段由用户在编辑器中验证并反馈。

## 进度总览

| 阶段 | 状态 |
|------|------|
| 方案与 API 核实 | ✅ 已完成 |
| 阶段 1：放下贴地与按钮 | ✅ 已完成 |
| 阶段 2：轮盘控制与退出 | ✅ 已完成（初版） |
| 阶段 2.5：启动后持续自转 | ✅ 已完成 |
| 阶段 2.6：碰撞与污渍清理 | 🟡 已实现，待引擎验收 |
| 阶段 3：多人和异常清理 | ⬜ 待开始 |
